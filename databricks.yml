bundle:
  name: mobilidade_urbana_pipeline

workspace:
  host: https://dbc-d96765b5-9a55.cloud.databricks.com

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      root_path: /Workspace/Users/${workspace.current_user.userName}/mobilidade_urbana_pipeline

  prod:
    mode: production
    workspace:
      root_path: /Workspace/production/mobilidade_urbana_pipeline